services:
  nginx:
    container_name: nginx
    build: requirements/nginx
    networks:
      - wordpress
      - adminer
      - monitoring
    depends_on:
      wordpress:
        condition: service_started
      website:
        condition: service_started
    restart: always
    init: true
    ports:
      - "443:443"
    volumes:
      - wordpress_data:/var/www/html:ro
    secrets:
      - ssl_certificate
      - ssl_key
    healthcheck:
      test: ["CMD", "curl", "-k", "-f", "https://localhost:443/"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 15s

  mariadb:
    container_name: mariadb
    build: requirements/mariadb
    networks:
      - database
    restart: always
    volumes:
      - mariadb_data:/var/lib/mysql
      - ./requirements/mariadb/conf:/docker-entrypoint-initdb.d/
    secrets:
      - mariadb_root_password
      - mariadb_database
      - mariadb_user
      - mariadb_password
    healthcheck:
      test: ["CMD", "mariadb-admin", "ping", "-h", "localhost"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 30s

  wordpress:
    container_name: wordpress
    build: requirements/wordpress
    networks:
      - wordpress
      - database
    depends_on:
      mariadb:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: always
    secrets:
      - mariadb_database
      - mariadb_user
      - mariadb_password
      - wp_admin
      - wp_public_user_password
    environment:
      - DOMAIN=${DOMAIN}
    volumes:
      - wordpress_data:/var/www/html

  website:
    container_name: website
    build: requirements/bonus/static-website
    networks:
      - wordpress
    restart: always
    init: true

  redis:
    container_name: redis
    build: requirements/bonus/redis
    networks:
      - wordpress
      - database
    restart: always
    init: true
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s

  vsftpd:
    container_name: vsftpd
    build: requirements/bonus/vsftpd
    networks:
      - wordpress
    depends_on:
      wordpress:
        condition: service_started
    restart: always
    ports:
      - "21:21"
    secrets:
      - ftp_user
      - ftp_password
    volumes:
      - wordpress_data:/var/www/html

  adminer:
    container_name: adminer
    build: requirements/bonus/adminer
    networks:
      - database
      - adminer
    depends_on:
      mariadb:
        condition: service_healthy
    restart: always

  cadvisor:
    container_name: cadvisor
    build: requirements/bonus/cadvisor
    networks:
      - monitoring
    restart: always
    init: true
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /dev/disk/:/dev/disk:ro   

  prometheus:
    container_name: prometheus
    build: requirements/bonus/prometheus
    networks:
      - monitoring
    depends_on:
      cadvisor:
        condition: service_started
    restart: always
    volumes:
    - ./requirements/bonus/prometheus/conf/prometheus.yml:/etc/prometheus/prometheus.yml:ro

  grafana:
    container_name: grafana
    build: requirements/bonus/grafana
    depends_on:
      prometheus:
        condition: service_started
    networks:
      - monitoring
    restart: always
    init: true
    secrets:
      - grafana_admin_user
      - grafana_admin_password
    volumes:
      - ./requirements/bonus/grafana/conf/datasources:/usr/share/grafana/conf/provisioning/datasources:ro
      - ./requirements/bonus/grafana/conf/dashboards/dashboard.yml:/usr/share/grafana/conf/provisioning/dashboards/dashboard.yml:ro
      - ./requirements/bonus/grafana/conf/dashboards/json:/usr/share/grafana/conf/provisioning/dashboards/json:ro
      - grafana_data:/var/lib/grafana

volumes:
  mariadb_data:
    name: mariadb
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/gueberso/data/mariadb
  wordpress_data:
    name: wordpress
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/gueberso/data/wordpress
  grafana_data:
    name: grafana
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/gueberso/data/grafana

networks:
  database:
    driver: bridge
    internal: true
  wordpress:
    driver: bridge
  adminer:
    driver: bridge
    internal: true
  monitoring:
    driver: bridge
    internal: true

secrets:
  mariadb_database:
    file: ../secrets/mariadb/mariadb_database.txt
  mariadb_password:
    file: ../secrets/mariadb/mariadb_password.txt
  mariadb_root_password:
    file: ../secrets/mariadb/mariadb_root_password.txt
  mariadb_user:
    file: ../secrets/mariadb/mariadb_user.txt
  wp_admin:
    file: ../secrets/wordpress/wp_admin.txt
  wp_public_user_password:
    file: ../secrets/wordpress/wp_public_user_password.txt
  ssl_certificate:
    file: ../secrets/ssl/cert.pem
  ssl_key:
    file: ../secrets/ssl/key.pem
  grafana_admin_user:
    file: ../secrets/grafana/grafana_admin_user.txt
  grafana_admin_password:
    file: ../secrets/grafana/grafana_admin_password.txt
  ftp_user:
    file: ../secrets/vsftpd/ftp_user.txt
  ftp_password:
    file: ../secrets/vsftpd/ftp_password.txt
